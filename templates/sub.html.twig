{% extends 'base.html.twig' %}

{% block body %}
<div class="col-12 mx-auto mt-5 row">
    <div class="col-6">
        <h3 class="text-white">Autheur: {{ subject.auhor.username }}</h3>
    </div>
    <div class="col-6">
        <h3 class="text-white">Date: {{ subject.createAt|date('d/m/Y') }}</h3>
    </div>
    <div class="col-6">
        {{ subject.body|raw }}
    </div>

</div>
{% endblock %}